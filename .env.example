# Copy this file to .env and adjust if campusone-api isn't running at the default local port.

# Base URL of the campusone-api backend, including its /api/v1 prefix.
VITE_API_BASE_URL=http://localhost:4000/api/v1
